import { Card } from '@mantine/core';

const TicketInformation = () => {
	// TODO: Fetch ticket related information:
	// Departure time, arrival time, departure location, arrival location, price, seat number, restrictions etc.
	return (
		<Card withBorder p={24}>
			<h2>Ticket Information</h2>
		</Card>
	);
};

export default TicketInformation;
